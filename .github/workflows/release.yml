name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          cache: true

      - name: Build the project
        run: cargo build --release

      - name: Rename binary (Linux)
        if: runner.os == 'Linux'
        run: mv target/release/ns-emu-mod-downloader target/release/ns-emu-mod-downloader-linux

      - name: Rename binary (macOS)
        if: runner.os == 'macOS'
        run: mv target/release/ns-emu-mod-downloader target/release/ns-emu-mod-downloader-macos

      - name: Upload binary as artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ runner.os }}
          path: |
            target/release/ns-emu-mod-downloader-*
            target/release/ns-emu-mod-downloader.exe
          if-no-files-found: ignore

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Upload binaries to release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
